char findAddedCharacter(string &s, string &t) {
    int n = s.size(), m = t.size();
    for (int i = 0; i < n; i++) {
        if (s[i] != t[i]) {
            return t[i];
        }
    }
    return t[m - 1];
}
