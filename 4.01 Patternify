#include <bits/stdc++.h> 
vector<string> printPatt(int n)
{
    // Write your code here
    vector<string> ans;
    for(int i=0;i<n;i++){
        string s="";
        for(int j=i;j<n;j++){
            s+='*';
        }
        ans.push_back(s);
    }
    
    
   return ans;
}
