#include <bits/stdc++.h> 
int hotelBookings(vector<string> queries) {
    // Write your codes here.
    int count = 0;

    for (auto & query: queries) {
        if (query[0] == '+') {
            count++;
        }
    }

    return count;
}
